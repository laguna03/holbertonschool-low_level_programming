#include <stdio.h>
/**
 * main- Program for printing a line with puts function
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"Programmming is like building a multilingual puzzle");
return (0);
}
